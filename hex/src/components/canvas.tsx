import { styled } from '@mui/material';

export default styled('canvas')({
  position: 'absolute',
  width: '100%',
  height: '100%',
  top: 0,
  bottom: 0,
  left: 0,
  right: 0,
  touchAction: 'none',
});
